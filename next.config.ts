import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Standard Next.js App Router deploy on Vercel.
  // Cloudflare/vinext worker bindings are not used in this production path.
};

export default nextConfig;
