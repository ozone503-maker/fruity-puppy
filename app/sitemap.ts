import type {MetadataRoute} from "next";import {blogArticles} from "./blog-data";const base="https://fruity-puppy.jessiesponberg.chatgpt.site";export default function sitemap():MetadataRoute.Sitemap{return ["","/our-team","/our-dream","/our-home","/safety-and-transparency","/lava-guava","/thorny-toad","/moon-tea","/fpx","/our-partners","/blog"].map((path)=>({url:`${base}${path}`,changeFrequency:"monthly" as const,priority:path===""?1:.8})).concat(blogArticles.map((article)=>({url:`${base}/f/${encodeURIComponent(article.slug)}`,lastModified:new Date(article.published),changeFrequency:"yearly" as const,priority:.7})))}
