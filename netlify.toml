# Static public/ fallback only. Prefer Vercel for the Next.js App Router app (see README).

[build]
  command = "true"
  publish = "public"

[build.environment]
  NODE_VERSION = "22"

[[headers]]
  for = "/images/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
